//! Argument parser — placeholder.
//!
//! Phase 7 will introduce the real CLI grammar (`ah do <wire>`,
//! `ah shell`, `ah tap`, `ah screenshot`, `ah install`, etc.). The
//! current stub is enough to keep the binary compiling.

/// Placeholder CLI args. Real parsing lands in Phase 7.
#[derive(Debug, Default, Clone)]
pub struct Args;