//! Stub — filled in Phase 4.
//!
//! Will wrap `IAccessibilityManager` + `AccessibilityNodeInfo` to dump
//! the UI tree, walk nodes, and run `AccessibilityNodeInfo.ACTION_*`
//! operations against selectors.