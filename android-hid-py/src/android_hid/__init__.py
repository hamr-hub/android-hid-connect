"""android_hid — Python SDK placeholder.

Phase 1 ships a pure-Python stub so `pip install -e .` produces an
importable package. Phase 7 will replace this with the real PyO3
binding via `maturin develop`.
"""

__version__ = "0.1.0"